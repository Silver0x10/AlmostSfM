#include "bundle_adjustment.h"

namespace pr {
    
    void bundle_adjustment(vector<Camera>& cameras, map<int, Vec3f> landmarks){
        // auto boh = (landmarks.begin());
        // cout << boh->first << endl;
    }

}